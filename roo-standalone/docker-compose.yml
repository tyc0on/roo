services:
  roo:
    build: .
    ports:
      - "80:8000"
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./skills:/app/skills:ro
    dns:
      - 8.8.8.8
      - 8.8.4.4
